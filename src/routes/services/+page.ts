import type { PageLoad } from '../$types';

export const load: PageLoad = async ({ parent }) => {
	// const { supabase } = await parent();
	// let { data: tableData, error } = await supabase.from('category_service_junction').select('*');
	// return { tableData };
};
